import { Component } from '@angular/core';

@Component({
  selector: 'homework-docusign-s3-root',
  template: `
    <h1>{{title}}</h1>
  `,
})
export class AppComponent {
  title = AppComponent.name;
}
